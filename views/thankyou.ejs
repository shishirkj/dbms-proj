<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You</title>
    <style>
        /* Body styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /* Container styles */
        .container {
            text-align: center;
        }

        /* Header styles */
        h1 {
            color: #333;
            font-size: 32px;
            margin-bottom: 20px;
        }

        /* Thank you message styles */
        .thank-you-message {
            font-size: 24px;
            color: #555;
            margin-bottom: 30px;
        }

        /* Link styles */
        a {
            color: #007bff;
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Thank you for your feedback</h1>
        <p class="thank-you-message">We appreciate your time and contribution.</p>
        <!-- You can add more content or links here if needed -->
    </div>
</body>
</html>
